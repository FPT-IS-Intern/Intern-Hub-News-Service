# 05 - Infra Module

Infra implements persistence for news:
- JPA entity: `News`
- JPA repository: `NewsJpaRepository`
- mapper: `NewsMapper`
- adapter: `NewsRepositoryImpl`

This module depends on `core` and Spring Data JPA.

