# 09 - Known Inconsistencies

Current base intentionally stays minimal.

Potential improvements:
- replace `IllegalArgumentException` with domain-specific exceptions
- add pagination for `GET /news`
- add integration tests for API + persistence

